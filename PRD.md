# 번역 비교 앱 — PRD

**팀**: 강력번역팀 (이유미 · 강진아)
**작성 근거**: `번역비교앱_제작프롬프트모음.docx` (제작 프롬프트 모음집 역산 정리) → 2026-07-30 제작 인터뷰 반영해 갱신

> **변경 이력**: 최초 버전은 3개 번역기(DeepL/파파고/구글) 데모 화면을 목표로 했으나, 제작 인터뷰를 통해 **"다양한 실제 번역 API의 품질을 비교하고, 최종 선택한 번역을 노션에 기록하는 것"**이 앱의 진짜 목적임이 확인되어 목표·범위·기술 스택을 갱신함.

---

## 1. 개요

일반 번역 API(DeepL, MyMemory)와 AI(LLM) 번역 API(Gemini, Groq 등)를 한 화면에서 동시에 호출해 결과를 비교하고, 마음에 드는 번역을 노션 데이터베이스에 기록해 나가는 개인용 도구. 예쁜 화면보다 **① 실제 번역 API 품질 비교, ② 노션 아카이빙**이 우선순위다. (Google/파파고/MS Translator/카카오는 카드 등록 부담·서비스 종료 등으로 보류 — §5, §10 참고)

## 2. 목표

- 카드 등록 없이 쓸 수 있는 일반 번역 API(DeepL·MyMemory) + AI 번역 API 2~3종(Gemini·Groq 등)을 실제로 호출해 한 화면에서 동시에 비교한다.
- 번역기 수가 늘어나도(최대 6~7개) 필요한 것만 켜고 끌 수 있게 해서 무료 API 할당량을 아낀다.
- 마음에 드는 번역을 고르면, 버튼 클릭으로 노션 데이터베이스에 원문·선택한 번역기·번역 결과를 저장한다.

## 3. 오늘의 완성 기준 (MVP)

입력 → 켜져 있는 번역기들에 실제 API 호출 → 결과 비교 → 하나 선택 → **"노션에 저장" 버튼으로 실제 노션 행 생성**까지 동작하면 완성. 로컬 히스토리·내보내기 문서(md)·Part 2 특색 아이디어는 여유가 있을 때 추가한다.

**MVP 구현 완료 (2026-07-31)**. 이후 실사용 중 추가된 요구사항도 같은 세션(야간 폴리싱 라운드)에서 전부 구현 완료됨:
- **언어 선택 — 구현 완료**: ko/en/ja/zh/es/fr/de/vi 8개 언어 중 원본/번역 언어를 드롭다운으로 자유롭게 선택 가능(스왑 버튼 포함). DeepL/MyMemory는 각 서비스 언어 코드로 매핑, LLM 계열은 프롬프트에 대상 언어명을 넣어 처리. (DeepL은 베트남어 미지원이라 해당 언어쌍 요청 시 카드에만 안내 메시지 표시, 나머지 엔진은 정상 동작 — §13 참고)
- **노션 저장 다중 선택 — 구현 완료**: "선택한 번역기"를 여러 엔진 동시 선택해 함께 저장 가능. 노션 스키마도 Select→Multi-select로 변경 완료 (§8)
- **QA에서 발견된 버그 수정 — 전부 완료**: 노션 저장 시 원문이 입력창 실시간 값을 쓰던 버그(번역 시점 값으로 고정), 저장할 번역기 id가 검증 없이 노션에 그대로 들어가 가짜 옵션이 생기던 버그(화이트리스트 검증 추가), 엔진 호출에 타임아웃이 없던 문제(15초 타임아웃 추가), LLM 엔진 출력 토큰 상한이 없어 응답이 폭주하던 문제(실측 OpenRouter 114,435자 → `max_tokens`/`maxOutputTokens: 1024` 적용), MyMemory 500자 초과 시 raw HTML 에러 노출 문제(사전 길이 체크 + 친절한 에러 메시지), 잘못된 언어코드/엔진id가 조용히 무시되던 API 검증 누락(400 에러로 명시적 거부)

## 4. 타겟 사용자

- 1차 사용자는 제작자 본인 — 로컬에서 혼자 쓰는 번역 품질 비교·아카이빙 도구
- 신조어/구어체처럼 번역기마다 결과가 갈리는 지점에 관심 있는 사용자

## 5. 번역 엔진 구성

| 구분 | 엔진 | 비고 |
|---|---|---|
| 일반 번역 API | DeepL | |
| 일반 번역 API | MyMemory | API 키/가입 불필요, 익명 하루 5,000자 · 이메일 파라미터 사용 시 하루 50,000자 무료. 카드 등록 없이 바로 연동 가능 |
| ~~일반 번역 API~~ | ~~Microsoft Translator~~ | 카드 등록 필요(가입 시 자동 종량제 전환 구조) + 사용자가 카드 등록을 원치 않아 보류 |
| 일반 번역 API | ~~Google Cloud Translation~~ | 카드 등록 부담으로 보류 |
| 일반 번역 API | ~~네이버 파파고~~ | 카드 등록 필요 + 자동 종량 청구 구조라 보류 |
| ~~일반 번역 API~~ | ~~카카오 번역~~ | 2022-07 서비스 종료, 사용 불가 |
| AI(LLM) 번역 API | Gemini | 상시 무료 티어 (카드 등록 불필요) — 기본 포함 |
| AI(LLM) 번역 API | Groq (Llama/Qwen 등 오픈소스 모델) | 상시 무료 티어 (카드 등록 불필요) — 기본 포함 |
| AI(LLM) 번역 API | Claude | 신규 가입 시 $5 1회성 크레딧만 제공(14일 후 소멸, SMS 인증 필요) — 우선 스킵, 나중에 여유 있을 때 추가 |
| AI(LLM) 번역 API | OpenRouter (`:free` 모델 로테이션) | 카드 등록 불필요, 무료 모델 분당 20회/일 200회. UI에는 "OpenRouter" 카드 1개로 표시하되, 내부적으로 2~3개 무료 모델(Llama/Mistral/Qwen 등)을 순서대로 시도해 한도 초과 시 자동 전환. 실제 응답한 모델명은 카드에 작게 표시 |

- 무료로 계속 쓸 수 있는 조합은 **Gemini + Groq**. Claude는 상시 무료가 아니므로 크레딧 소진 시 빠질 수 있음을 감안한다.
- API 키를 추가로 구할 수 있는 번역 서비스가 있으면 계속 확장 가능한 구조로 설계한다 (엔진 목록을 배열/설정 파일로 관리).

## 6. 핵심 기능

### 6.1 비교 화면 (구현 완료)
- 활성화된 모든 번역 엔진을 항상 나란히 비교 표시 (카드 그리드, 모바일은 세로 스택) — `app/page.tsx`, `components/EngineCard.tsx`
- 각 엔진 카드에 **켜기/끄기 토글**을 제공해 호출할 엔진을 사용자가 직접 선택 (무료 할당량 절약 목적)
- 입력창 위에 **원본 언어 / 번역 언어 선택 드롭다운**(스왑 버튼 `⇄` 포함) 배치. 한국어↔영어 고정이 아니라 ko/en/ja/zh/es/fr/de/vi **8개 언어** 중 자유롭게 언어쌍을 바꿀 수 있음(`lib/engines/config.ts`의 `SUPPORTED_LANGUAGES`). DeepL/MyMemory는 각 서비스의 언어 코드로 매핑(`lib/engines/deepl.ts`, `mymemory.ts`), LLM 계열(Gemini/Groq/OpenRouter)은 프롬프트에 대상 언어명을 넣어 처리(`lib/engines/prompt.ts`)
- 카드 구성: 엔진 이름, 번역 결과(+ 실제 응답한 모델명 표시), "이 번역 선택" 버튼(**다중 선택 가능한 토글** — 여러 카드를 동시에 선택 상태로 둘 수 있음, 한 개만 고르도록 강제하지 않음, 선택된 카드는 파란 테두리+체크 아이콘으로 구분)
- 특정 엔진 호출 실패 시 해당 카드에만 사용자 친화적 에러 메시지 표시(기술적 원문 노출 없음, `lib/engines/errors.ts`), 나머지는 정상 동작
- 로딩 중 스켈레톤 표시, 토글/버튼에 `aria-label`/`aria-pressed` 등 접근성 속성 반영

### 6.2 번역 로직 (구현 완료)
- 공통 인터페이스: `async function getTranslations(text, sourceLang, targetLang, enabledEngineIds)` → `{ [engineId]: { text?, model?, error? } }` (`lib/engines/index.ts`)
- **일반 번역 API(DeepL, MyMemory)**: 각 서비스 공식 REST API를 Next.js API Route(서버 사이드)에서 호출 (MyMemory는 키 없이 바로 호출 가능, 500자 초과 시 사전 차단 + 안내 메시지)
- **AI 번역 API(Gemini/Groq/OpenRouter)**: "다음 문장을 자연스럽게 번역해줘" 형태의 단순 프롬프트로 호출 (Claude는 아직 미구현 — §5, §10 참고)
- **OpenRouter는 내부적으로 모델 로테이션**: 무료 모델 3종(`lib/engines/openrouter.ts`의 `FREE_MODELS`)을 순서대로 시도, 실패/한도초과(429 등) 시 다음 모델로 자동 전환. 응답에 실제 사용된 모델명을 포함해 카드에 표시
- 모든 fetch 호출에 **15초 타임아웃**(`AbortSignal.timeout`), LLM 3종(Gemini/Groq/OpenRouter)은 **출력 토큰 상한 1024**(`max_tokens`/`maxOutputTokens`)로 응답 폭주 방지
- 활성화된(켜진) 엔진만 병렬 호출 (`Promise.all`), 꺼진 엔진은 호출하지 않음
- API Route(`app/api/translate/route.ts`)에서 빈 텍스트/3000자 초과/미지원 언어코드/알 수 없는 엔진id를 400 에러로 명시적 거부
- 모든 API 키는 서버 사이드(API Route)에서만 사용, `.env.local`에 보관 — 프론트엔드에 절대 노출 금지

### 6.3 선택 & 노션 저장 (구현 완료)
- 카드별 "이 번역 선택" 버튼 → 하이라이트, **여러 카드를 동시에 선택 가능(다중 선택)**, 재클릭 시 개별적으로 선택 해제
- 선택 후 별도의 **"노션에 저장" 버튼**(선택된 개수 표시)을 눌러야 실제로 저장됨 (자동 저장 아님 — 실수 저장 방지). 선택한 엔진 중 아직 번역이 완료되지 않은 게 있으면 버튼 비활성화 + 사유 안내
- 저장 항목: 속성은 원문(**번역 요청 시점 값으로 고정** — 저장 시점에 입력창을 수정해도 영향받지 않음, `app/page.tsx`의 `translatedText` 상태) / 선택한 번역기(**다중 선택된 엔진 목록**, Notion Multi-select, 화이트리스트 검증 후 저장) / 저장 시각 3개뿐이고, **그 순간 호출됐던 모든 엔진의 결과는 페이지 본문에 제목+문단으로 정리해서 백업**(결과 있는 엔진만, 선택된 엔진은 ✅ 표시) — 나중에 노션에서도 전체 비교가 가능하면서 가독성도 좋도록 함 (2026-08-01 재설계, §8 참고)
- 저장 성공/실패 상태를 안내 문구로 표시, **성공 시 저장된 노션 페이지로 바로가기 링크** 제공
- **향후 방향(로그인 도입 시, 2026-08-01 논의)**: 지금은 `.env.local`의 `NOTION_API_KEY`/`NOTION_DATABASE_ID`가 제작자 본인의 워크스페이스 하나에 고정된 **Internal Integration** 방식이라, 모든 저장이 이 한 곳으로만 간다. 나중에 여러 사용자가 각자 자기 노션에 저장하게 하려면, Notion 개발자 포털에서 **Public Integration(OAuth)**으로 전환해서 로그인 시 Notion 인증 화면으로 리디렉션 → 사용자가 직접 워크스페이스/페이지 접근을 승인 → 그 사용자 전용 액세스 토큰을 발급받는 구조로 바꿔야 한다. 이건 ①회원가입/로그인 시스템, ②사용자별 토큰을 저장할 실제 백엔드 DB(지금은 없음, localStorage/노션뿐), ③Notion Integration을 Internal→Public으로 전환하는 작업이 함께 필요한 큰 확장이라, 로그인 시스템 자체를 만들 때 §6.4의 로컬 히스토리 이전 방향과 함께 다시 설계한다. 로그인 시스템 자체는 아직 계획에 없음.

### 6.4 로컬 히스토리 (구현 완료)
- 노션이 메인 저장소이므로 필수는 아니었으나, `localStorage` 기반으로 최근 저장 기록 20개까지 브라우저에 남겨 새로고침해도 유지되도록 구현함 (`lib/history.ts`). 처음엔 "번역기들" 탭 하단에 UI가 있었으나, 탭 재편(§15)으로 최상위 **"최근 기록"** 탭으로 분리됨(`components/tabs/HistoryTab.tsx`)
- **향후 방향(로그인 도입 시)**: 지금은 브라우저(기기) 단위 로컬 저장이라 다른 기기/브라우저에서는 기록이 안 보인다. 나중에 로그인 기능을 추가하면, 이 히스토리를 `localStorage`에서 **사용자 계정에 귀속된 서버/DB 저장소로 이전**해서 "내 기록"으로 어디서든 볼 수 있게 하는 게 맞다 (원안 `번역비교앱_제작프롬프트모음.docx` Phase2 로드맵의 "사용자별 선호 설정: localStorage → 추후 로그인 연동 시 DB로 이전" 방향과 일치). 로그인 시스템 자체는 아직 계획에 없음 — 실제 도입 시점에 다시 설계.

## 7. 특색 아이디어 (탭 구조로 제공, 사용 여부는 사용자가 그때그때 판단)

**아키텍처 결정 (2026-07-31, 2026-08-01 갱신)**: §6의 번역 비교 화면(문서에서 관용적으로 "기본 비교"라고 부르지만, 실제 코드상 최상위 탭 라벨은 **"번역기들"**, `app/page.tsx`의 `TABS`)은 이미 완성된 핵심 플로우이므로 특색 아이디어로 어지럽히지 않는다. ①~⑥은 전부 "번역기들"의 번역 결과를 그대로 활용해 실행되는 하위 기능이므로, **최상위 탭이 아니라 "번역기들" 안의 하위 탭**으로 둔다 — "결과 비교"(기본값, 지금까지 만든 화면 그대로)와 아래 6개 특색 아이디어 하위 탭으로 분리(최상위 탭 = 번역기들 / 오타 변환기 / 이미지 텍스트 추출 / 최근 기록, 뒤 3개는 서로 독립적인 유틸리티라 그대로 최상위에 둠). 이하 §7·§15에서 "기본 비교"라고 쓴 곳은 모두 이 "번역기들" 탭을 가리킨다.
- 아직 로직을 안 만든 탭은 아이디어 설명 + "아직 준비 중" 안내만 표시하는 **자리(placeholder)**로 둔다 — 전부 미리 만들 필요 없음, 쓰고 싶은 것만 나중에 하나씩 살을 붙인다.
- 원문/번역 언어(`sourceLang`/`targetLang`)와 방금 번역한 결과(`translatedText`, 엔진별 결과)는 **탭을 앱 최상위(`app/page.tsx`) 상태로 공유**해서, ①번역 신뢰도 배틀 뷰·②역번역 체크처럼 "방금 비교한 결과"를 그대로 재활용해야 하는 탭이 다시 번역을 요청하지 않고 바로 쓸 수 있게 한다.
- ⑤투표/공유형 결과처럼 별도 백엔드(realtime DB)가 필요한 것은 탭 안에서 "Phase 2 예정" 안내만 하고 실제 기능은 만들지 않는다.

| # | 기능명 | 핵심 내용 | 구현 방식 |
|---|---|---|---|
| ① | 번역 신뢰도 배틀 뷰 | 여러 결과를 어절 단위로 비교해 불일치 구간을 색상으로 하이라이트, 일치율 스코어 표시 | 순수 프론트엔드 로직, 공유 상태의 번역 결과 재사용 — API 의존성 없어 가장 구현 쉬움 — **구현 완료** |
| ② | 역번역 체크 | 성공한 번역 결과 전체를 원언어로 역번역해 원문과 나란히 비교 | **재구현 완료 (2026-08-01)** (`components/tabs/BackTranslationTab.tsx`, `lib/backTranslate.ts`, `app/api/back-translate/route.ts`). 이전 버전(어절 겹침 기반 "의미 보존율 %")은 어순만 달라도 낮은 점수가 나오는 등 나이브한 지표라는 게 실사용으로 확인되어 폐기 — 자동 점수/배지 없이 원문/번역 결과/역번역 결과 3단을 나란히 보여줘 사람이 직접 눈으로 판단하게 함. 호출 방식도 엔진별 개별 역번역(최대 5회 호출)에서 **배치 1회 호출**로 바꿈: 성공한 번역 결과 전체(최대 5개)를 한 번에 "역번역 담당 엔진" 1곳에 몰아서 요청 — 1순위 DeepL(`text` 파라미터를 배열로 보내 진짜 배치 API 호출), 실패 시 2순위 Gemini(번호 매긴 프롬프트 1회 호출 + 번호 기준 파싱), 그마저 실패하면 3순위 Groq(같은 방식)로 폴백. 실제로 역번역을 수행한 엔진을 "역번역: DeepL" / "역번역: Gemini (DeepL 실패)"처럼 표시. "번역기들" 탭(하위 탭 "결과 비교")에서 성공한 결과가 1개 이상이면 "전체 결과 역번역으로 검증 →" 버튼이 나타나고, 이 버튼을 눌러야만(자동 실행 없음) 탭 전환 + API 호출이 함께 일어남. |
| ③ | 신조어/유행어 감지 배지 | 입력 문장의 신조어·밈 표현을 AI로 감지, 엔진별 반영 여부 비교 | LLM 엔진에 감지용 프롬프트 추가 호출 필요 |
| ④ | 번역기별 캐릭터화 | 각 엔진에 페르소나 부여, 캐릭터 말투 코멘트 | 프롬프트 추가 호출 필요 |
| ⑤ | 투표/공유형 결과 | 선택 결과 공유 링크 + 방문자 투표 통계 (Phase 2 성격, realtime DB 필요) | 탭 안내만, 실제 기능은 보류 |
| ⑥ | 문맥 슬라이더 | 반말↔격식체↔비즈니스체 톤 슬라이더로 재번역 비교 | **구현 완료 (2026-08-05, 재설계)** (`components/tabs/ToneSliderTab.tsx`). 처음엔 "결과 비교"의 번역 결과를 그대로 가져와 재번역하는 연동형이었으나, "결과 비교처럼 텍스트를 붙여넣고 여러 사이트에서 검색하는 용도로 쓸 것 같다"는 피드백으로 완전히 독립된 입력창(자체 원문/언어/엔진 결과 상태)을 갖는 형태로 재설계함. 엔진 목록·켜기끄기·순서(`enabled`/`engineOrder`/`isEngineOrderLocked`)는 "결과 비교"와 그대로 공유해서 "같은 사이트, 같은 버튼"으로 통일. 별도 API/오케스트레이션 모듈 없이 기존 `/api/translate`에 optional `tone` 파라미터만 추가해 그대로 재사용(`lib/engines/index.ts`의 `getTranslations`, `lib/engines/prompt.ts`의 `buildTranslationPrompt`). DeepL/MyMemory는 프롬프트를 못 받는 고정 API라 tone을 그냥 무시하고 평소처럼 번역하고, LLM 3종(Gemini/Groq/OpenRouter)만 실제로 톤을 반영함. 슬라이더는 3단(반말/격식체/비즈니스체) 세그먼트 버튼으로 구현하되, "결과 비교"와 달리 톤 버튼은 선택만 하고 "번역하기" 버튼을 눌러야 호출됨(버튼도 통일). **톤은 다중 선택 가능**(체크박스처럼 동작) — "하나만 보이니 비교하기 불편하다"는 피드백으로, 엔진 카드 하나에 선택된 톤 전부의 결과를 나란히 쌓아서 보여줌. DeepL/MyMemory는 톤을 무시하므로 1번만 호출해 그 결과를 선택된 톤 전부에 복제하고, LLM 3종은 선택된 톤 개수만큼 병렬 호출해 실제로 다른 결과를 받음(`app/page.tsx`의 `handleTranslateTone`). |

## 8. 노션 데이터베이스 설계 (구현 완료, 2026-08-01 재설계)

실제 데이터베이스(`번역기들`)에 적용 완료함. `NOTION_DATABASE_ID`는 `.env.local`에 저장됨.

**재설계 배경**: 처음엔 엔진별 결과 전체를 속성(property) 칸에 다 넣었더니, 노션에서 페이지를 열었을 때 안 쓴 엔진까지 "비어 있음"으로 쭉 나열되고 서식(제목/구분)도 못 줘서 읽기 나쁘다는 피드백을 받음. 속성은 필터/정렬용 최소 메타데이터만 남기고, 실제 비교 결과는 페이지 본문에 제목+문단으로 정리하는 구조로 변경.

**속성 (테이블 뷰에서 보이는 것, 3개만)**

| 속성명 | 타입 | 설명 |
|---|---|---|
| 원문 | Title | 번역 요청한 원본 문장 |
| 선택한 번역기 | Multi-select | DeepL / MyMemory / Gemini / Groq / OpenRouter / Claude |
| 저장 시각 | Date | 노션에 기록된 시각 |

**페이지 본문 (`app/api/notion/route.ts`에서 `children` 블록으로 작성)**

- `## 번역 결과 비교` (heading_2)
- 결과가 있는 엔진만(에러/빈 값이면 항목 자체를 안 만듦) 엔진마다 `### {라벨}` (선택된 엔진은 `### ✅ {라벨} (선택됨)`) + 결과 텍스트 문단
- 기존에 있던 엔진별 결과 컬럼 6개(DeepL 결과/MyMemory 결과/...)와 "선택한 번역 결과" 컬럼은 **삭제함** — 기존에 저장돼 있던 행들의 해당 칸 데이터도 함께 사라짐(사용자 확인 후 진행한 되돌릴 수 없는 작업). 실제 저장 테스트로 속성 3개 + 본문에 결과 있는 엔진만 정확히 나열되는 것 확인.
- **제목 길이 처리**: 원문(제목)은 길이 보장이 없어 그대로 쓰면 목록 스캔이 어렵다는 재피드백으로, **제목은 60자로 자르고(초과 시 "…") 전체 원문은 본문 맨 위 `## 원문` 아래에 그대로 보존**(`truncateTitle`, `app/api/notion/route.ts`). 60자 넘는 실제 문장으로 저장 테스트해 제목만 줄고 본문엔 전체가 남는 것 확인.

## 9. 기술 스택

- **프레임워크**: Next.js (App Router, TypeScript, Tailwind CSS) — 프론트(React 컴포넌트)와 백엔드(API Route)를 한 프로젝트에서 관리, API 키를 서버 사이드에 안전하게 보관하기 위해 선택
- **번역 API 연동**: 엔진별 클라이언트 모듈 분리 (`lib/engines/deepl.ts`, `mymemory.ts`, `gemini.ts`, `groq.ts`, `openrouter.ts`), 공통 타입(`types.ts`), 클라이언트에서도 안전한 메타데이터·언어 목록(`config.ts`), LLM 공용 프롬프트 빌더(`prompt.ts`), 레지스트리(`index.ts`)로 구성. 공통 에러 처리 모듈(`lib/engines/errors.ts`)이 타임아웃/HTTP 상태코드/네트워크 오류를 사용자 친화적 한국어 메시지로 변환하고 기술적 상세는 서버 콘솔에만 로그. `openrouter.ts`는 내부에 무료 모델 목록을 배열로 두고 순차 시도(로테이션/폴백) 로직을 포함. Claude/Google/Papago/MS Translator는 나중에 추가하기로 하면 같은 패턴으로 모듈만 추가하면 됨(레지스트리에 한 줄 등록)
- **언어 지원**: ko/en/ja/zh/es/fr/de/vi 8개 언어(`lib/engines/types.ts`의 `LanguageCode`), 엔진별 언어 코드 매핑은 각 모듈에 내장(DeepL은 베트남어 미지원)
- **노션 연동**: `@notionhq/client`, Next.js API Route에서 서버 사이드로 호출, 엔진 id ↔ Multi-select 옵션 라벨 화이트리스트로 가짜 옵션 생성 방지
- **실행 환경**: 로컬 개발(`npm run dev`) 우선, 추후 배포 여부는 미정 (배포 시 Vercel 등 고려 가능)
- **환경변수**: 모든 API 키/토큰은 `.env.local`에 보관, 저장소에 커밋하지 않음

## 10. API 키 및 외부 서비스 준비 현황

발급 진행 상황을 관리하기 위한 체크리스트. 실제 키 발급은 다음 세션에서 하나씩 함께 진행한다.

| 서비스 | 발급처 | 무료 여부 | 상태 |
|---|---|---|---|
| DeepL | DeepL API 가입 페이지 | 무료 티어 있음(월 50만자) | **발급 완료** (`.env.local`) |
| MyMemory | (가입/키 불필요) | 상시 무료 (익명 5,000자/일, 이메일 지정 시 50,000자/일) | **연동 준비 완료** (키 불필요) |
| ~~Microsoft Translator~~ | Azure Portal | 무료 F0 티어 있음(월 200만자, 상시) | 보류 (카드 등록 거부) |
| ~~Google Cloud Translation~~ | Google Cloud Console | 신규 크레딧 있음, 이후 유료 | 보류 (카드 등록 부담으로 스킵) |
| ~~네이버 파파고~~ | Naver Cloud Platform | 카드 등록 필요 + 무료 한도 불명확 | 보류 (자동 종량 청구 구조라 스킵) |
| ~~카카오 번역~~ | (해당 없음) | - | 서비스 종료(2022-07)로 사용 불가 |
| Gemini | Google AI Studio | 상시 무료 티어 | **발급 완료** (`.env.local`, 기존 키 재사용) |
| Groq | Groq Console | 상시 무료 티어 | **발급 완료** (`.env.local`) |
| Claude (선택) | platform.claude.com | 신규 $5 크레딧(1회성, 14일 소멸) | 보류 (여유 있을 때 발급) |
| OpenRouter | openrouter.ai/keys | 카드 등록 불필요, 무료 모델 분당 20회/일 200회 | **발급 완료** (`.env.local`, 기존 키 재사용) |
| LibreTranslate (스트레치) | 로컬 셀프호스팅(Docker) | 완전 무료·무제한, 외부 결제 계정 불필요, 한국어 지원 확인됨 | 미도입 — Docker 설치 필요해 나중에 여유 있을 때 추가 |
| Apertium (검토 후 보류) | 공개 API/자체 서버 | 무료 | 한국어 지원 불확실(유럽어 중심)해 제외 |
| Argos Translate (검토 후 보류) | 로컬 Python 라이브러리 | 무료 | API가 아닌 Python 라이브러리라 Next.js 연동에 별도 브릿지 필요, 제외 |
| Hugging Face 번역 모델 (검토 후 보류) | Hugging Face Inference API | 무료지만 요청 제한 빡빡함 | 설정 난이도 대비 이득 낮아 제외 |
| Notion | notion.so → Integrations (Integration명: `translators`) | 무료 | **발급·연결 완료** — 키/페이지 ID/데이터베이스 ID 전부 `.env.local`에 저장, 페이지 연결(공유)까지 확인됨 |

## 11. 비기능 요구사항

- API 키는 절대 프론트엔드 코드/커밋에 노출하지 않는다 — 반드시 Next.js API Route(서버 사이드) 경유
- 무료 할당량 소진을 막기 위해 기본적으로 필요한 엔진만 켜서 호출 (전체 켜기는 사용자가 명시적으로 선택)
- 번역기 1개 실패가 나머지 비교를 막지 않도록 개별 에러 처리
- 노션 저장은 명시적 버튼 클릭으로만 발생 (자동 저장 금지)
- 모바일 반응형 고려 (필수도는 낮음, 개인용 로컬 도구이므로 데스크탑 우선)

## 12. 성공 기준

- [x] 켜져 있는 번역 엔진들에 대해 실제 API 응답이 화면에 동시에 표시된다 (mock이 아닌 실제 연동, 5개 엔진 모두 검증됨).
- [x] 엔진 켜기/끄기 토글이 실제로 호출 여부를 제어한다.
- [x] 번역 하나 이상을 선택하고 "노션에 저장" 버튼을 누르면 실제 노션 데이터베이스에 새 행이 생성된다 (다중 선택 지원).
- [x] 원본/번역 언어를 8개 언어 중에서 자유롭게 선택할 수 있다.
- [x] QA에서 발견된 버그(원문 고정, 엔진id 화이트리스트, 타임아웃, 토큰 상한, MyMemory 에러 노출, API 검증 누락)가 모두 수정되었다.

## 13. 리스크 및 오픈 이슈

남아 있는 실제 리스크만 정리함 (이번 야간 라운드에서 고친 버그는 §3·§12로 이동, 목록에서 제거).

- **무료 API 할당량 소진 가능성**: Gemini/Groq/OpenRouter/MyMemory 모두 무료 티어 한도가 있어, 사용량이 늘면 특정 엔진이 일시적으로 막힐 수 있음. 개별 엔진 에러로만 처리되고 전체 앱은 죽지 않지만, 사용자가 한도 초과를 인지하기 어려울 수 있음(에러 메시지로 안내 중)
- **OpenRouter 무료 모델 로테이션 목록의 수명**: `lib/engines/openrouter.ts`의 `FREE_MODELS` 3종은 OpenRouter 쪽 라인업 변경(단종/이름 변경)에 취약함 — 404/429가 잦아지면 `https://openrouter.ai/api/v1/models`에서 재확인 필요 (CLAUDE.md에도 기록)
- **Claude 엔진 미구현**: 신규 가입 시 1회성 $5 크레딧(14일 소멸)만 제공해 상시 무료가 아니므로 이번 범위에서 제외함. 노션 데이터베이스 스키마(§8)와 `app/api/notion/route.ts`의 라벨 매핑에는 이미 자리를 마련해둠 — 여유 있을 때 `lib/engines/claude.ts`만 추가하면 됨
- **DeepL의 베트남어(vi) 미지원**: 8개 언어 중 DeepL만 vi를 지원하지 않아, vi가 sourceLang/targetLang에 포함되면 DeepL 카드에는 "지원하지 않는 언어쌍" 안내만 표시되고 나머지 4개 엔진은 정상 동작함 — 의도된 부분 실패이지 버그 아님
- Google Cloud Translation / 네이버 파파고 / Microsoft Translator는 카드 등록 부담으로 계속 보류 상태 (§5·§10, 언제든 재검토 가능)
- 카카오 번역 API는 2022-07 서비스 종료로 선택지에서 완전히 제외
- Part 2 특색 아이디어(§7)는 이번 목적(품질 비교 + 노션 저장)과 직접 관련이 적어 우선순위를 낮춤

## 14. 다음 단계 (완료됨)

준비 단계(API 키 발급 + 노션 설정) 및 실제 연동(**DeepL, MyMemory, Gemini, Groq, OpenRouter**)까지 모두 완료됨.

1. [x] Next.js 프로젝트 스캐폴딩
2. [x] 엔진 모듈 구현 (`lib/engines/deepl.ts`, `mymemory.ts`, `gemini.ts`, `groq.ts`, `openrouter.ts`) + 켜기/끄기 토글 UI
3. [x] 노션 저장 API Route 구현 (`@notionhq/client`, `NOTION_DATABASE_ID`로 속성 3개(원문·선택한 번역기(다중)·저장 시각) + 페이지 본문에 엔진별 결과 백업 기록, §8 참고)

## 15-0. 실제 화면 시각 QA (Playwright 스크린샷, 2026-07-31)

Playwright로 실제 브라우저 화면을 캡처해서(`.qa-screenshots/`, git 미포함) 아래 상태를 육안으로 검토함: 초기 화면/입력/결과/다중선택/배틀뷰 탭/준비중 탭/토글 OFF/토글 OFF 후 번역/모바일(375px)/저장 성공/에러 상태(빈 입력). 콘솔·페이지 에러(`pageerror`)는 전 과정에서 0건.
- 토글 ON/OFF, 다중 선택 하이라이트, 배틀 뷰 하이라이트, 에러 안내, 저장 성공 링크 — 전부 겹침/깨짐 없이 정상 렌더링 확인.
- **발견된 이슈(정정, 2026-08-01)**: 모바일 폭(375px)에서 좌측 하단에 떠 있던 검은 원형 배지는 처음엔 브라우저 확장 프로그램으로 추정했으나, 재확인 결과 **Next.js 개발 모드 자체의 dev 인디케이터**("Compiling.." 등)였음 — 프로덕션 빌드(`npm run build && npm run start`)에서는 나타나지 않는 프레임워크 내장 UI라 실사용자에게는 영향 없음. 앱 코드 수정 불필요.

## 15. 완료된 작업 이력 (주제별 정리)

원래 이 섹션은 하루 동안의 실사용 피드백을 시간순으로 1~16번 매긴 체인지로그였다. 스캔하기 어려워져 **주제별로 재정리**함 — 정보 손실 없이 축약하되, 실측 로그·시행착오 설명은 줄이고 **왜 그렇게 결정했는지(이유)** 는 남겼다. 원래 번호는 각 항목 끝 `(구 N번)`으로 남겨 과거 참조와 연결할 수 있게 했다.

### 탭 구조 / 레이아웃
- **특색 아이디어 탭 껍데기 구축 + ①배틀 뷰 구현** — 탭 UI(기본 비교 + 특색 아이디어 6개) 구축, ①번역 신뢰도 배틀 뷰 실제 구현, ②~⑥은 "준비 중" placeholder. (구 5번)
- **UI 리디자인** — makingsoftware.com 톤 참조해 크림 배경 + 코발트 블루 포인트 + 블루프린트(도면) 패널 스타일로 전체 리디자인. 다크모드는 이 톤과 상충해 의도적으로 제거하고 단일 라이트 테마로 고정. 제목 Do Hyeon, 본문 Noto Serif KR, 라벨/버튼 IBM Plex Mono. (구 6번)
- **탭 계층 구조 재편 — 특색 아이디어를 최상위 → "번역기들" 하위 탭으로** — "①~⑥은 전부 번역기들 결과를 기반으로 동작하는 하위 기능"이라는 피드백으로, 최상위 탭을 **번역기들 / 오타 변환기 / 이미지 텍스트 추출 / 최근 기록**(서로 독립적인 유틸리티)으로 줄이고, ①~⑥(결과 비교가 기본값)은 "번역기들" 안의 **하위 탭(`BASIC_SUB_TABS`, `activeSubTab`)**으로 이동. 탭 전환 콜백/문구도 전부 하위 탭 기준으로 갱신. (구 14번)
- **액션 버튼 재배치** — 번역 전에도 "노션에 저장"(비활성) 버튼만 있는 빈 박스가 최상단에 뜨는 UX 안티패턴이라는 피드백으로 제거. **"전체 결과 역번역으로 검증 →"**는 "번역하기" 버튼 옆(왼쪽 입력 패널)으로. **"노션에 저장"**은 카드 그리드 아래 → "카드가 길면 하단으로 밀려 안 보인다"는 재피드백으로 **입력 패널 안(번역하기/역번역검증 버튼 바로 아래)**으로 재배치. "지우기" 버튼/다중선택 안내 문구도 같은 이유로 그리드 아래 → **그리드 위(하위 탭 띠 바로 아래)**로 이동. (구 15번)
- **탭 계층 시각적 재설계 — "붙어있는 다이얼로그" 느낌** — 상위/하위 탭이 같은 pill 스타일이라 계층이 안 보인다는 피드백(Windows 다이얼로그의 탭+그룹박스 참고)으로, 최상위 탭은 활성 시 패널과 같은 배경색 + `-mb-px`로 겹쳐 "패널에 붙은 폴더 탭"처럼 보이게 하고, 하위 탭은 패널 안 첫 줄에 연한 액센트 배경 띠(세그먼트 컨트롤)로 배치. **후속 수정 2건**: 활성 탭 배경(흰색)과 하위탭 띠(연한 파랑) 색이 달라 경계선이 보였던 것 → 색 통일. 부모 `gap-6`이 탭 `<nav>`와 `tabpanel` 사이 24px 틈을 남겼던 것 → `tabpanel`에 `-mt-6`으로 상쇄. (구 16번)

### 노션 저장
- **중복 저장 방지 가드** — 같은 내용으로 "노션에 저장"을 연타하면 중복 행이 생기던 문제 → `app/api/notion/route.ts`에 10초 윈도우 인메모리 dedupe 가드(원문+언어쌍+선택엔진 해시 키) 추가, 연속 저장 테스트로 중복 미생성 확인. (구 1번. 노션 스키마 자체의 속성/본문 재설계는 §8에 별도 정리)

### 로컬 히스토리
- **로컬 히스토리 구현** — `lib/history.ts` + `localStorage`(`translation-history`, 최근 20개). 처음엔 "번역기들" 탭 하단에 UI가 있었으나, 위 탭 재편에서 **최상위 "최근 기록" 탭**으로 완전히 분리됨 — "번역기들 화면에 자리를 차지할 이유가 없다"는 피드백 반영. (구 2번, §6.4)

### 엔진/API 점검
- **OpenRouter 무료 모델 목록 점검** — `FREE_MODELS` 3종(`inclusionai/ling-3.0-flash`, `nvidia/nemotron-3-nano-30b-a3b`, `google/gemma-4-31b-it`)이 여전히 `:free` 목록에 있고 실제로 응답하는지 `/models` 조회 + 직접 호출로 확인, 교체 불필요 판정. (구 4번, 상세 로그는 CLAUDE.md에 유지)

### 언어 처리
- **원본 언어 자동 감지** — 드롭다운에 "자동 감지" 추가. DeepL은 `source_lang` 생략, MyMemory는 `langpair=autodetect|...` 사용. 감지·target 조정·번역을 `/api/translate` 한 라우트에서 처리(별도 감지 라우트로 뺐다가 왕복 줄이려 재통합). **주의**: MyMemory 감지 호출의 target은 지원 8언어 밖 값("it")으로 고정해야 함 — target이 감지된 언어와 같으면 403(PLEASE SELECT TWO DISTINCT LANGUAGES)으로 실패하는 걸 실측으로 확인. (구 7번)
- **버그 수정 — targetLang을 일본어 등으로 골라도 무시되던 문제** (2026-08-05) — 원본 언어를 "자동 감지"로 두고 번역할 언어를 일본어로 골랐는데 실제로는 영어로 번역되는 버그 리포트로 발견. **원인**: `resolvedTargetLang` 계산이 "감지된 언어가 ko면 무조건 target=en, en이면 무조건 target=ko"였어서, targetLang을 ko/en이 아닌 다른 언어로 명시적으로 골라도 항상 덮어써졌음(`app/api/translate/route.ts`). **수정**: "감지된 언어와 targetLang이 완전히 같을 때만"(자기 자신으로 번역은 의미 없으므로) ko↔en 기본 페어로 대체하도록 조건을 하나로 통일 — 사용자가 고른 targetLang이 감지된 언어와 다르면 항상 그대로 존중함. 실제 호출로 (자동 감지+일본어 선택, 한국어 입력) → 일본어로 정상 번역되는 것과 기존 ko/en 자동 스왑 케이스 둘 다 회귀 확인.

### 특색 아이디어 ②역번역 체크 (§7 ②번 참고)
- **최초 구현** — 성공한 번역 전체를 원언어로 역번역해 비교하는 기능 최초 구현, 오타 변환기 옆(배틀 뷰보다 앞)에 배치. (구 9번)
- **재구현 — 배치 호출 + 나이브 점수 제거** — 최초 버전(`BackTranslateCheckTab.tsx`, 어절 겹침 기반 "의미 보존율 %", 엔진별 개별 역번역 최대 5회 호출)을 폐기. **왜**: 어순만 달라도 점수가 낮게 나오는 나이브한 지표라는 게 실사용으로 확인됐기 때문. → `BackTranslationTab.tsx` + `lib/backTranslate.ts`로 교체, 자동 점수 없이 원문/번역/역번역 3단 나란히 비교. 호출도 **배치 1회**로 통합: DeepL 배치(`translateBatchWithDeepl`) → 실패 시 Gemini 번호매김 배치 → 실패 시 Groq 같은 방식. 정상 케이스(DeepL 1회 호출로 3문장 역번역)와 강제 실패 폴백 케이스(`vi` 언어쌍으로 DeepL 실패시켜 Gemini가 실제로 이어받음) 둘 다 실제 호출로 검증. "번역기들" 탭에는 성공한 결과가 1개 이상이면(선택 여부 무관) "전체 결과 역번역으로 검증 →" 버튼 표시. (구 11번)

### 유틸리티 탭 추가
- **한글 오타 변환기 탭** — 사용자의 다른 레포(`Coding_26.07_typoConverter`)를 `lib/typoConverter.ts`로 이식, 서버 API 없이 동작하는 순수 클라이언트 탭. (구 8번)
- **이미지 텍스트 추출(OCR) 탭** — 마우스로 텍스트를 못 긁어오는 스크린샷/사진 상황을 위한 독립 유틸리티. 전용 OCR 서비스(카드 등록 필요) 대신 이미 쓰는 **Gemini의 이미지 입력(멀티모달)을 재사용**해 새 키 없이 구현(`extractTextFromImage`). `app/api/ocr/route.ts`(mimeType 화이트리스트 4종, 용량 상한 검증) + `components/tabs/OcrTab.tsx`(Ctrl+V/파일 선택 → 추출 텍스트 수정 가능 → "번역기들로 보내서 번역하기"). 실제 텍스트 이미지로 추출 정확도 확인, 400 검증도 확인. (구 12번)
- **OCR 폴백 추가 (OpenRouter)** — Gemini 실패(한도초과 등) 시 OpenRouter로 폴백. 번역용 `FREE_MODELS`와 **겹치지 않는 별도 비전 모델**(`OCR_FREE_MODELS`: `nemotron-nano-12b-v2-vl` 1순위 → `nemotron-3-nano-omni-30b-a3b-reasoning` 2순위) 사용 — **왜**: 번역/OCR이 같은 모델의 무료 한도를 나눠 쓰지 않게 하려고. 실측: vl 모델은 표현을 과교정하는 경향(예: "adorkable"→"adorable"), omni-reasoning은 원문에 더 가깝지만 추론 과정이 섞여 느림 → vl을 1순위, omni-reasoning을 2순위로 배치. 응답에 실제 추출 엔진 표시. (구 13번)
- **OCR 탭 UX 개선** (2026-08-01) — ①결과 텍스트박스를 `rows={4}` 고정(내부 스크롤 발생)에서 내용 길이에 맞춰 자라는 방식으로 변경(`textareaRef` + `scrollHeight` 기반 높이 재계산, "번역기들" 결과 카드처럼 스크롤 없이 자연스럽게 늘어남). ②히스토리 탭이 제공하던 "마크다운 다운로드" 기능을 OCR 결과에도 추가 — 중복 구현하지 않고 `downloadTextFile`을 `lib/download.ts`로 공용 분리해 `HistoryTab.tsx`/`OcrTab.tsx` 양쪽에서 재사용. ③붙여넣은 이미지를 지울 수 있는 "✕" 버튼 추가. ④"이미지가 이미 있는데 '이미지 파일 선택' 버튼이 계속 보이는 건 버그 아니냐"는 지적 — 여러 이미지를 동시에 다루는 기능은 의도한 적 없어 실제 버그였음. ③번 지우기 버튼과 함께 해결: 이미지가 있으면 "파일 선택" 버튼을 숨기고 "✕ 지우기"만 보이게 해서, 새 이미지를 넣으려면 먼저 지우고 다시 넣는 흐름으로 통일. Playwright로 4가지 전부 실제 화면 확인.
- **콩글리시 찾기 탭** (2026-08-05~06) — "키친, 카드, 핸드폰처럼 한국식으로 부르는 영어 표현의 진짜 영어 단어를 네이버/구글 대신 앱 안에서 찾고 싶다"는 요청. 처음엔 Gemini 단독 지식으로 구현했으나 "네이버"를 입력했는데 "사이다" 답이 나오는 등 실제 입력과 무관한 답변이 나오는 사례가 발견됨 — 원인 조사 중 그날 이미 Gemini(분당 요청 한도)·Groq(하루 토큰 한도)를 다른 기능 테스트로 다 써버린 상태였던 게 확인돼, "LLM 자체 지식보다 근거 있는 답"을 원한다는 피드백과 맞물려 아래처럼 재설계함:
  1. **네이버 백과사전 검색**(`lib/engines/naver.ts`의 `searchEncyclopedia`)으로 관련 항목(제목+요약)을 가져와 근거로 삼음. 여러 검색 API 후보(네이버 블로그/지식iN, SerpApi 등) 중 "더 근거 있는" 백과사전을 사용자가 직접 선택, SerpApi는 무료 한도(월 250회)가 네이버 자체 API(하루 25,000회)보다 훨씬 적어 채택 안 함.
  2. 검색 결과가 있으면 그 내용을 근거로 **OpenRouter 무료 모델 로테이션**(`completeWithOpenRouter`, `lib/engines/openrouter.ts`)이 기존과 같은 형식(로마자 표기/영어 표현/설명/예문)으로 정리(`buildKonglishPromptWithContext`). Gemini·Groq는 한도가 빡빡해서 이 기능에서는 완전히 제외(기존 `findRealEnglish`/`findRealEnglishWithGroq` 삭제).
  3. 검색 결과가 없으면 OpenRouter가 자체 지식으로 답변(`buildKonglishPrompt`).
  4. OpenRouter까지 실패하면 (검색 결과가 있었다면) 백과사전 검색 결과 원문을 그대로 보여주고, 없었다면 에러.
  - **네이버 API 인증 삽질**: 사용자가 NCP(Naver Cloud Platform)의 "NAVER API HUB" 콘솔에서 키를 발급받았는데, 예전 개발자센터(openapi.naver.com) 방식(`X-Naver-Client-Id`/`Secret` 헤더)으로 호출하면 401이 남. 실제로는 완전히 다른 도메인/헤더(`https://naverapihub.apigw.ntruss.com/search/v1/encyc`, `X-NCP-APIGW-API-KEY-ID`/`X-NCP-APIGW-API-KEY`)를 써야 함 — 실제 호출로 원인 특정 후 수정, 재호출로 정상 응답(원피스 검색 결과 3건) 확인.
  - **응답 완결성 검증 추가**(`lib/konglish.ts`의 `isCompleteAnswer`) — 모델이 형식은 지켰지만 값 없이 라벨만 되풀이하는 응답을 "성공"으로 잘못 판단해 빈 칸을 그대로 보여주던 버그를 발견해 수정. 4개 필드가 실제로 채워졌는지 확인 후 아니면 다음 우선순위로 넘어감.
  - **알아둘 점**: 이 검증은 "형식은 맞는데 완전히 다른 주제를 답하는" 경우까지는 못 잡음. 2026-08-06 재현 테스트 중 이런 사례(예: "네이버" 입력에 "ㅋㅋㅋ/lol" 답변)가 실제로 관찰됐는데, 조사해보니 그 시점에 Gemini·Groq·OpenRouter 무료 모델 셋 다 그날 누적 테스트로 한도/속도제한에 걸려 있었음(서버 로그에 타임아웃·429 다수 확인) — 공급자 쪽이 과부하 상태일 때 벌어지는 현상으로 보이고, 코드 버그로 재현 가능한 것은 아직 못 잡음. 한도가 회복된 정상 상태에서 추가로 검증 필요(다음 세션에서 재확인할 것 — 백로그에도 기록).

### UX 다듬기 / 리팩터링 (포니테일 리뷰 반영)
- "번역기들" 탭에 선택된 번역이 있으면 "역번역 체크"로 바로 이동하는 링크 추가.
- `BattleViewTab`/`BackTranslateCheckTab`에 중복돼 있던 상수(`VERDICT_STYLE`/`VERDICT_MARK`/`VERDICT_TITLE` → `lib/battleView.ts`)와 안내 패널(→ `components/TabEmptyNotice.tsx`) 통합.
- 클라이언트가 감지→번역 2단계로 직접 오케스트레이션하던 걸 `/api/translate` 한 번 호출로 통합(`app/api/detect-language` 라우트 삭제).
- 다른 참고 앱 캡처에서 착안해 예문 체험 칩(클릭 시 입력창 자동 채움)과 저장 기록 "클립보드 복사"/"마크다운 다운로드" 버튼 추가(`lib/history.ts`의 `formatHistoryAsMarkdown`).
(구 10번)

### 포니테일 + 멀티 에이전트 종합 리뷰 (2026-08-01)
`agents/` 폴더의 페르소나(QA·백엔드아키텍트+AI연동·프론트엔드+UX)로 3라운드 병렬 리뷰 진행, 전부 **치명적/중요 이슈 없음**으로 결론. 하루 동안 매우 잦은 라이브 리팩터링으로 쌓였을 법한 죽은 코드만 소소하게 발견해 정리:
- `app/page.tsx`의 `saveState` 타입을 인라인으로 중복 선언 → `BasicCompareTab.tsx`가 export하는 `SaveState` 타입을 import해서 재사용하도록 통합.
- `SUPPORTED_LANGS`(8개 언어 화이트리스트)가 `app/api/translate/route.ts`와 `app/api/back-translate/route.ts`에 각각 하드코딩 중복 → 둘 다 `lib/engines/config.ts`의 `SUPPORTED_LANGUAGES`에서 파생하도록 통합(언어 추가/제거 시 한 곳만 고치면 됨).
- `lib/engines/index.ts`의 `ENGINES` 배열이 모듈 내부에서만 쓰이는데 `export` 키워드가 붙어 있어 죽은 public API였음 → 내부 전용으로 정리.
- 삭제된 `BackTranslateCheckTab.tsx`를 참조하는 코드가 남아있는지 grep으로 재확인 — 흔적 없음(정상).
- 탭 재편 이전 이름을 참조하던 오래된 QA 스크립트(`scripts/qa_backtranslate.mjs`, 실행하면 깨지는 상태로 방치돼 있었음) 삭제. 이 파일이 유일한 ESLint 경고(미사용 변수) 발생원이었어서, 삭제 후 `npm run lint`가 경고 0건으로 완전히 깨끗해짐.
- `ComingSoonTab.tsx`의 "②~⑥번 공용 placeholder" 주석이 ②번(역번역 체크) 실구현 이후에도 안 고쳐져 있던 것 정정.
- 3라운드 전부 실제 API 호출(translate/notion/back-translate/ocr) + Playwright 스크린샷으로 검증하며 진행, `npm run build`/`lint` 매 라운드 재확인.

### PWA 전환 (2026-08-01)
휴대폰/데스크톱에 앱처럼 설치할 수 있도록 PWA로 전환. 외부 라이브러리(`next-pwa` 등) 추가 없이 Next.js 내장 기능 + stdlib만으로 구현:
- `app/manifest.ts` — Next.js의 Metadata Files API로 `/manifest.webmanifest`를 자동 생성(별도 `<link rel="manifest">` 추가 불필요). `name`/`icons`/`theme_color`(`--accent` `#2b48d9`)/`background_color`(`--paper-card` `#fffdf6`)/`display: "standalone"`.
- `public/icons/icon-192.png`, `icon-512.png`, `apple-touch-icon.png` — 이미지 라이브러리(sharp 등) 없이 Node 내장 `zlib`만으로 PNG를 직접 인코딩하는 일회성 스크립트로 생성(스크립트 자체는 산출물만 남기고 삭제). 앱 색상 팔레트로 만든 "겹친 카드"(번역 비교를 상징) 아이콘.
- `public/sw.js` — 최소 서비스 워커. 앱 셸(`/`)만 오프라인 폴백용으로 캐시하고, 번역/OCR/노션 등 API 요청과 정적 자원은 캐싱하지 않고 그대로 네트워크로 통과시킴 — 실시간 API 응답을 캐시하면 오래된 결과를 보여줄 위험이 있어(앱의 진짜 목적이 실제 API 비교라서) 일부러 캐시 범위를 최소화함.
- `components/ServiceWorkerRegister.tsx` — 서비스 워커 등록만 담당하는 별도 클라이언트 컴포넌트, `app/layout.tsx`(서버 컴포넌트)에 부수효과 없이 얹음.
- `app/layout.tsx`의 `metadata.appleWebApp`/`metadata.icons.apple` + `viewport.themeColor`(Next.js 최신 버전은 `themeColor`가 `metadata`가 아니라 별도 `viewport` export로 분리됨)로 iOS 홈 화면 추가 지원.
- Playwright로 실제 서비스 워커 등록(`active: true`) + manifest 링크 + theme-color + apple-touch-icon 전부 정상 확인, 콘솔 에러 0건.

### 접속자/설치 수 추적 (2026-08-01)
Vercel에 배포 후 링크를 다른 사람에게 공유한 상태 — 무료 API 사용량과 직결되는 "오늘 접속자가 몇 명인지"를 알고 싶다는 요청. 커스텀 DB/로깅 파이프라인을 새로 만들지 않고, 이미 Vercel에 배포돼 있다는 전제를 활용해 **Vercel Web Analytics**(호스팅에 포함된 1st-party 기능)로 해결:
- `@vercel/analytics` 설치, `app/layout.tsx`에 `<Analytics />` 추가 — 일별 방문자 수/페이지뷰는 이걸로 자동 수집됨(Vercel 프로젝트 대시보드의 Analytics 탭에서 확인).
- **주의**: 코드만으로는 수집이 시작되지 않는다. Vercel 프로젝트 대시보드 → Analytics 탭에서 Web Analytics를 활성화해야 실제로 데이터가 쌓이기 시작함(Vercel CLI/OAuth 연동이 이 세션에 없어 내가 대신 켤 수 없음 — 사용자가 직접 켜야 함).
- `components/InstallTracker.tsx` — PWA "설치한 사람 수"도 알고 싶다는 후속 요청에 대응. 브라우저의 `appinstalled` 이벤트를 감지해 `track("pwa_installed")` 커스텀 이벤트로 Vercel Analytics에 기록. **iOS Safari는 `appinstalled` 이벤트 자체가 없어(홈 화면에 추가를 감지할 JS API가 없음) iOS 설치는 집계되지 않음** — 안드로이드/데스크톱 크롬·엣지 등에서만 정확한 카운트.
- 로컬(`next start`)에서는 Vercel Analytics 스크립트(`/_vercel/insights/script.js`)가 Vercel 엣지망에서만 서빙되는 경로라 404가 나는 게 정상 — 배포 환경에서만 실제로 동작함(Playwright로 로컬 404 확인, 기능 오류 아님을 검증).
- 로그인 시스템이 아직 없어 "누가"까지는 특정할 수 없고, 트래픽 볼륨(몇 명/몇 회)만 확인 가능 — 개인 식별이 필요해지면 로그인 기능([[백로그]]의 "로그인 시 접속기록" 항목) 도입 시 같이 재검토.

### 앱 이름/아이콘 실사용 자산으로 교체, 방문자 수 앱 내 노출 (2026-08-01)
- 앱 이름을 "번역 비교 앱"/"번역비교"에서 **"번역기들"**로 통일(브라우저 탭 제목, 매니페스트 `name`/`short_name`, 페이지 상단 `<h1>`, iOS `appleWebApp.title` 전부) — 이미 최상위 탭 이름이 "번역기들"이라 일관성 확보.
- 사용자가 제공한 실제 로고 이미지(겹친 문서+체크 아이콘, 앱 팔레트와 일치)로 플레이스홀더 아이콘을 교체. 리사이즈는 Next.js 의존성 트리에 이미 설치돼 있던 `sharp`를 그대로 재사용(새 의존성 추가 없음): `app/icon.png`(256px)/`app/apple-icon.png`(180px)는 Next.js의 Metadata Files 컨벤션으로 자동 `<link>` 태그 생성(수동 `metadata.icons` 설정 제거, 기존 `app/favicon.ico`도 이걸로 대체되어 삭제), `public/icons/icon-192.png`/`icon-512.png`는 매니페스트(`app/manifest.ts`)용으로 유지.
- **방문자 수를 노션 대시보드가 아니라 앱 화면 안에도(거슬리지 않게) 노출**해달라는 요청 — `components/VisitCounter.tsx`가 화면 맨 아래에 아주 작고 흐린 글자(`text-foreground/30`)로 "오늘 방문 N명"만 표시. 브라우저 세션당 1회만 증가 요청을 보내고(세션스토리지로 중복 방지), 그 외엔 조회만 함. 연동이 안 됐거나 실패하면 조용히 아무것도 렌더링하지 않음(깨진 UI 대신 완전히 숨김).
- **저장소는 처음에 노션으로 만들었다가 Supabase로 교체함** — 사용자가 "매일 갱신되는 단순 숫자를 왜 문서 도구(노션)에 기록하냐"고 정확히 지적함. 노션은 원자적 증가가 안 돼 읽고-다시-쓰는 방식이라 동시 방문 시 카운트가 씹힐 수 있고, MCP로 만든 DB가 앱의 내부 통합과 별도 권한 공유까지 필요했음 — 단순 카운터엔 안 맞는 도구였다는 게 실제로 증명된 사례. `supabase/schema.sql`(사용자가 Supabase SQL Editor에서 1회 실행)의 `increment_visit` RPC가 upsert+증가를 단일 SQL 문으로 처리해 진짜 원자적 증가를 보장함. `app/api/visit/route.ts`는 `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`(서버 전용, `.env.local`)로 `@supabase/supabase-js` 사용.
- 이전에 만들었던 노션 "방문 기록" DB는 이제 안 씀 — 사용자가 노션에서 수동으로 지워도 되고 그냥 둬도 무해함(더 이상 참조하는 코드 없음).
- 사용자가 직접 Supabase 프로젝트 생성 + 스키마 실행 완료. Vercel Production 환경변수는 처음엔 사용자가 대시보드에서 등록을 놓쳐서 실제 배포 사이트가 `{count:null}`을 반환했음 — 사용자가 발급한 임시 Vercel API 토큰으로 `POST /v10/projects/{id}/env`(Production 타깃) 호출해 두 값을 직접 등록하고, `POST /v13/deployments`(기존 배포 참조 재배포)로 즉시 반영, 실제 배포 도메인에서 증가(1)/조회 API 호출로 최종 확인함. 테스트 호출로 쌓인 당일 카운트는 매번 확인 후 0으로 초기화해둠.
- **본인(운영자) 방문은 집계에서 빠져야 한다는 지적** — 실제로 사용자 본인이 테스트 삼아 3번 들어간 게 그대로 카운트에 잡힘. `components/VisitCounter.tsx`에 운영자 전용 URL 파라미터(`?owner=1`)를 추가 — 한 번 이 파라미터로 접속하면 그 브라우저에 `localStorage` 플래그가 영구 저장되고, 이후로는 그 브라우저의 방문이 증가 요청을 보내지 않음(조회만 함, 카운터 자체는 계속 보임). 세션스토리지가 아니라 로컬스토리지라 브라우저를 재시작해도 계속 제외됨 — 본인이 쓰는 기기/브라우저마다 한 번씩 `https://<배포주소>/?owner=1`로 접속해두면 됨.

### 특색 아이디어 ⑥문맥 슬라이더 (§7 ⑥번 참고, 2026-08-05)
- **최초 구현** — 반말/격식체/비즈니스체 3단 톤으로 재번역하는 탭 최초 구현. "결과 비교"에서 이미 번역한 원문(`translatedText`)을 그대로 가져와 재번역하는 연동형으로 만들고, LLM 3종(Gemini/Groq/OpenRouter) 전용 API(`app/api/tone-translate/route.ts`)를 따로 둠.
- **재설계 — 독립 입력창 + 엔진/버튼 통일** — "결과 비교와 연동해 쓰기보다, 결과 비교처럼 텍스트를 붙여넣고 여러 사이트에서 검색하는 방향으로 쓸 것 같다"는 피드백을 받음. **왜**: 사용자가 실제로 원하는 사용 흐름은 "번역기들 결과 재활용"이 아니라 "톤 비교만 독립적으로 해보고 싶을 때 바로 텍스트를 붙여넣는" 것이었기 때문. → ToneSliderTab을 "결과 비교"와 같은 레이아웃(언어 선택 드롭다운 + 자체 입력창 + "번역하기" 버튼 + 엔진 카드)의 완전히 독립된 도구로 재설계. **엔진 목록/켜기끄기/순서**(`enabled`/`engineOrder`/`isEngineOrderLocked`)는 "결과 비교"와 상태 자체를 공유해 두 탭에서 항상 같은 엔진 구성이 보이도록 통일. 별도 API(`app/api/tone-translate/route.ts`)·오케스트레이션(`lib/toneTranslate.ts`)은 삭제하고, 기존 `/api/translate`에 optional `tone` 파라미터만 추가해 그대로 재사용(`lib/engines/index.ts`의 `getTranslations`가 tone을 각 엔진의 `translate()` 호출로 그냥 전달 — DeepL/MyMemory는 무시, LLM 3종만 반영). `components/EngineCard.tsx`의 `onSelect`를 optional로 바꿔 노션 저장용 "이 번역 선택" 버튼 없이도 같은 카드 컴포넌트를 재사용하게 함.
- 실제 호출로 검증(재설계 후): DeepL/MyMemory/Gemini/Groq/OpenRouter 5개 엔진 전부에 business 톤으로 "오늘 시간 되면 같이 밥 먹을래?"를 보내 DeepL/MyMemory는 톤 무시하고 평소처럼("Would you like to eat together when you have time today?"), Groq/OpenRouter는 비즈니스체로("Would you be available to have lunch together at your convenience today?") 응답하는 것 확인. tone 없는 일반 `/api/translate` 호출도 기존과 동일하게 동작하는 것 회귀 확인.
- **톤 다중 선택으로 변경** — "체크한 항목을 모두 볼 수 있게 폭을 늘리면 어떨까, 비교하려는데 하나만 보이니 불편하다"는 피드백. **왜**: 이 탭의 핵심 목적 자체가 "톤별 비교"인데 단일 선택은 그 목적에 안 맞았음. → `tone: ToneId` 단일 선택을 `selectedTones: ToneId[]` 다중 선택(체크박스처럼 토글, 기본값 3개 전부 선택)으로 변경. 결과 데이터 구조도 `Record<EngineId, CardState>`에서 `Record<EngineId, Partial<Record<ToneId, CardState>>>`(`ToneCardStateMap`)로 바꿔, 엔진 카드 하나 안에 선택된 톤 결과를 전부 나란히 쌓아서 보여줌.
- **DeepL/MyMemory를 탭에서 완전히 제외** — "톤 지원 안 되는 엔진을 뭐하러 두냐"는 피드백. **왜**: 처음엔 "사이트도 통일" 요청에 맞춰 5개 엔진을 다 보여줬으나, 톤이 반영 안 되는 2개를 그대로 두니 "3개 선택했는데 결과가 1개만 나온다"는 혼란만 유발하고 실효도 없었음. → `lib/engines/config.ts`에 `TONE_SUPPORTED_ENGINE_IDS`(gemini/groq/openrouter) 화이트리스트를 추가해 이 탭에는 그 3개만 렌더링·호출함(`ToneSliderTab.tsx`의 `visibleEngineOrder`, `app/page.tsx`의 `handleTranslateTone`). 드래그 순서 변경은 "결과 비교"와 공유하는 전체 5개짜리 `engineOrder` 배열 기준 인덱스를 그대로 써야 해서, 화면에 보이는 카드 번호(ENGINE 01~03)만 이 탭 기준으로 다시 매기고 실제 재정렬은 원본 배열 인덱스로 처리함. 실제 호출로 casual/business 두 톤을 동시에 선택해 Groq/OpenRouter가 각각 다른 톤의 결과를 반환하는 것 확인.
- **톤 표시 순서 고정 + 요청을 순차 실행으로 변경** — ① "반말/격식체/비즈니스체 순서가 클릭한 순서로 뒤죽박죽 보인다"는 리포트로, 카드 안 톤 순서를 `selectedTones`(클릭 순서) 대신 `TONE_OPTIONS`의 고정 순서로 정렬해서 렌더링하도록 수정. ② "3개 톤 중 하나만 '무료 사용량 한도 초과'로 실패한다"는 리포트로 원인 조사 — Gemini만 단독 호출해도 실패해서 이번 건 자체는 **그날 Gemini 일일 무료 할당량이 실제로 소진된 것**(버그 아님)으로 확인했지만, 조사 중 톤별 요청을 `Promise.all`로 **병렬** 실행하고 있어서 무료 티어 엔진에 톤 개수만큼(최대 3개) 동시 요청이 몰리는 구조적 리스크를 발견 → 톤별 요청을 순차 실행으로 바꿔(`app/page.tsx`의 `handleTranslateTone`) 결과가 도착하는 대로 하나씩 채워지게 하고 동시 요청 경합을 줄임.
- **"문맥 슬라이더"에도 역번역 체크 연결** — "결과물을 확인하려면 역번역할 수 있어야겠지?"라는 제안으로, "결과 비교"의 "전체 결과 역번역으로 검증 →" 버튼과 인프라(`/api/back-translate`, `lib/backTranslate.ts`)를 문맥 슬라이더에도 연결함. 엔진 하나가 톤별로 여러 결과를 낼 수 있어서 항목 id를 `"groq-casual"`처럼 엔진+톤 합성 키로 구분(`app/api/back-translate/route.ts`는 engineId를 문자열로만 다뤄 화이트리스트 검증이 없어 그대로 사용 가능). 켜진 엔진(최대 3) × 선택된 톤(최대 3) = 최대 9개 항목이 나올 수 있어 `MAX_ITEMS`를 5 → 10으로 올림. "결과 비교"/"문맥 슬라이더" 중 어느 쪽에서 트리거했는지에 따라 "역번역 체크" 화면의 원문 표시와 "다시 확인" 재요청 대상이 달라져야 해서 `backTranslateOriginalText`/`backTranslateSource` 상태로 구분(`app/page.tsx`). 실제 호출로 엔진×톤 합성 4개 항목을 한 번에 역번역해 정상 응답받는 것 확인.

### 남은 백로그 (다음 라운드)
- Claude API 키가 생기면 엔진 추가 (구 3번, 계속 보류)
- ③④⑤ 특색 아이디어 중 필요한 것 있으면 선택해서 구현 (⑥번은 구현 완료)
- 노션에 남은 테스트 데이터([QA 테스트], [백엔드 테스트], [중복테스트], [스크린샷 QA] 등 접두사) — 삭제 도구가 없어 자동 정리 못 함, 사용자가 수동으로 정리 필요
- 역번역 체크(②) 배치 폴백은 번호 매긴 프롬프트를 파싱하는 방식이라, 모델이 형식을 안 지키면(번호 누락 등) 그 엔진은 실패 처리하고 다음 순위로 넘어감 — 실제로 자주 발생하면 파싱 로직(`lib/engines/prompt.ts`의 `parseNumberedBatchResponse`)을 더 관대하게 다듬을 필요가 있음
- 로그인 기능을 만들 때 사용자 접속기록(access log)도 함께 추가할 것 (2026-08-01 사용자 요청, 지금은 로그인 자체가 없어 보류)
- **콩글리시 찾기 재검증 필요** (2026-08-06, 진행 중) — 하루 지나서 다시 테스트해봤는데 OpenRouter 무료 모델 3개가 **여전히** 전부 429(한도 초과) 상태였음 — 즉 "다음날이면 풀린다"는 예상과 달리, 이 무료 모델들은 내 앱만의 하루 한도가 아니라 OpenRouter 전체 사용자가 공유하는 상시 혼잡 상태일 가능성이 큼. 그래서 "입력과 무관한 답" 버그가 진짜 코드 문제인지는 아직도 깨끗하게 재현/반증 못 함 — 다만 `isCompleteAnswer` 검증 + 백과사전 원문 폴백 덕분에, 지금은 그 실패 상황에서도 엉뚱한 답 대신 깔끔한 에러나 실제 백과사전 검색 결과(원문)가 나오는 것까지는 확인함. OpenRouter 무료 모델이 계속 이 상태면, 포맷팅 단계를 다른 걸로 바꾸는 걸 고려할 것(예: 유료 소액 크레딧 충전, 혹은 이 정도 혼잡도면 그냥 백과사전 원문 표시를 기본으로 하는 것도 방법).
- **네이버 백과사전 사용량 85% 가드 추가** (2026-08-06) — 사용자 요청: 무료 한도(하루 25,000회)를 넘겨서 과금되는 상황을 원천적으로 막고 싶다고 함. `supabase/schema.sql`에 `api_usage_counts` 테이블 + `increment_api_usage` RPC 추가(방문자 수 카운터와 같은 원자적 증가 패턴 재사용, `lib/supabase.ts`로 클라이언트 생성 로직 공용화). `lib/engines/naver.ts`가 호출 전 오늘 사용량을 확인해 21,250회(85%) 이상이면 네이버를 건너뛰고 곧장 LLM 자체 지식 경로로 감. Supabase 카운터를 못 쓰는 상황(키 없음/DB 오류)이면 가드 자체를 건너뛰고 정상 호출—과금 방지가 목적이라 카운터 장애로 기능이 막히는 것보다는 카운터 없이 도는 쪽을 선택. **사용자가 Supabase SQL Editor에서 갱신된 `supabase/schema.sql` 전체를 다시 한 번 실행해야** 실제로 카운트가 쌓이기 시작함(`create table if not exists`/`create or replace function`이라 기존 부분을 다시 실행해도 안전).
- **콩글리시 결과 중 "영어 표현" 강조** (2026-08-06) — 답변에서 가장 중요한 값인 "영어 표현: xxx" 줄을 굵게 표시해달라는 요청. `KonglishTab.tsx`가 답변을 줄 단위로 나눠 렌더링하며 "영어 표현:" 줄만 `<strong>`으로 감쌈.
